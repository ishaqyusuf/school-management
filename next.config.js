/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  // i18n: {
  //   locales: ["en", "ar"],
  //   defaultLocale: "ar",
  // },
};

module.exports = nextConfig;
